// Session constants shared by edge middleware and server code. Keep Node-only session imports out
// of this module.

// Session cookie name. Changing it signs everybody out.
export const sessionCookie = 'dalnex_session';
